declare aws_uri="http://169.254.169.254/latest"

# metadata.get
# print JSON document describing metadata for the current AWS instance
metadata.get () {
    curl -s $aws_uri/dynamic/instance-identity/document | jq -r .
}
